# Copy this file to secrets.toml for local password testing, OR paste this
# single line into Streamlit Community Cloud → app → Settings → Secrets.
# secrets.toml itself is gitignored so the real password is never committed.

app_password = "change-me-to-a-real-password"
